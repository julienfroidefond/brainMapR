{
	"name": "brainR",
	"version": "0.0.1",
	"dependencies": {"bower": "^1.3.2",
	"express" : "3.x"},
	"devDependencies": {
	},
	"engines": {
	"node": ">=0.10.0"
	},
	"scripts": {
	    "postinstall": "bower cache clean && bower install && node app.js"
	}
}
